application: tgt-dev
version: 1
runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: main.app

- url: /reminder
  script: main.app
  login: admin

libraries:
- name: jinja2
  version: latest
